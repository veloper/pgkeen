# ------------------------------------------------------------------------------
# PG_VECTORIZE EXTENSION SETTINGS
# ------------------------------------------------------------------------------
# vectorize.embedding_service_url = ''      # URL for embedding service (e.g., 'http://localhost:3000/v1/embeddings')
# vectorize.openai_key = ''                 # OpenAI API key for LLM and embedding endpoints
# vectorize.openai_service_url = ''         # Override base URL for OpenAI-compatible endpoints (e.g., vLLM, custom proxy)
# vectorize.ollama_service_url = ''         # Base URL for Ollama LLM endpoints (e.g., 'http://localhost:3001')
# vectorize.database_name = ''              # Database for background worker (default: 'postgres')
# vectorize.batch_size = 100                # Batch size for embedding requests (default: 100)
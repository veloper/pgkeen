# ------------------------------------------------------------------------------
# PG_CRON EXTENSION SETTINGS
# ------------------------------------------------------------------------------
cron.database_name = 'postgres'         # Database where jobs are stored
# cron.host = ''                          # Hostname for background worker connections
# cron.port = 5432                        # Port for background worker connections
# cron.username = ''                      # Username for background worker connections
# cron.password = ''                      # Password for background worker connections
# cron.log_run = on                       # Log job runs (on/off)
# cron.log_statement = off                # Log job statements (on/off)
# cron.max_running_jobs = 10              # Max jobs running in parallel
# cron.job_run_timeout = 0                # Max seconds a job can run (0 = unlimited)
# cron.database_list = ''                 # Comma-separated list of databases to run jobs in